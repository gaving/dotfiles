# Commit, push and open the commit on github if possible
gcp() {
    gca -m $1 && gp && ghc
}

# Open up github page for latest commit
ghc() {
    LATEST_COMMIT=$(git log 2>/dev/null | head -n1 | awk '{print $2}')
    [[ -n $LATEST_COMMIT ]] && open http://github.com/gaving/${PWD##*/}/commit/$LATEST_COMMIT
}

